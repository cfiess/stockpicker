# Stock Screener — Python dependencies
# Install with: pip install -r requirements.txt

# ── HTTP / scraping ──────────────────────────────────────────────────────────
requests>=2.31.0            # HTTP client (Reddit, StockTwits, SEC EDGAR, Yahoo)
beautifulsoup4>=4.12.3      # HTML parsing (Finviz, fallback scrapers)
lxml>=5.1.0                 # Fast HTML/XML parser

# ── Environment ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # Load .env file

# ── Timezone ─────────────────────────────────────────────────────────────────
tzdata>=2024.1              # IANA timezone data (needed on some platforms)

# ── Optional: SMS delivery (only needed if you want text message output) ─────
# twilio>=9.0.0

# ── Optional: real-time price data (kept for future price enrichment) ─────────
# yfinance>=0.2.38
# pandas>=2.0.0
